---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<button
  class="mb-4 mt-3 block h-9 w-16 rounded-[50%] bg-gray-200 font-acumin text-xs font-[1000] italic text-black"
  type="submit">{text}</button
>
